# App
PORT=8080
TYPEORM_SYNC=true

# DB (RDS or local compose)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=webforx_store
DB_USER=postgres
DB_PASS=postgres
DB_SSL=false
# DB_SSL_CA_PATH=/app/global-bundle.pem

# Optional bootstrap (not for RDS)
CREATE_DB_IF_MISSING=false

# Optional S3 for images
S3_BUCKET=
S3_REGION=
STARTUP_UPLOAD_STATIC=false
# S3_ACCESS_KEY=
# S3_SECRET_KEY=

# Optional product seed override
# SEED_PRODUCTS_JSON=[{"name":"WFX Hat","price":12.5,"image":"strawberry.svg"}]
